# Dependencies to run neurotile tutorials standalone (python NN_<topic>/<file>.py).
# These are NOT required to use nkilib.experimental.neurotile in your own kernels —
# only for running the tutorial scripts in this directory.
#
# Install with:
#   pip install -r src/nkilib_src/nkilib/experimental/neurotile/examples/requirements.txt

torch_xla
torch
numpy
